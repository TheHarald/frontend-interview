// Задача: реализовать хук useDebounce(value, delay).
// - Возвращает значение, которое обновляется с задержкой delay мс после последнего изменения value.
// - При размонтировании или смене value/delay таймер должен очищаться (нет обновления после unmount).
//
// Пример: const [query, setQuery] = useState(''); const debouncedQuery = useDebounce(query, 300);


export function useDebounce(value, delay) {
  // TODO
}
