// Задача: реализовать хук useWindowSize().
// - Возвращает { width, height } — текущие размеры окна (window.innerWidth, window.innerHeight).
// - При изменении размера окна обновлять state. При размонтировании снять слушатель resize.
// - Желательно использовать throttle или debounce для resize, чтобы не вызывать setState слишком часто.
//
// Пример: const { width, height } = useWindowSize();

export function useWindowSize() {
  // TODO
}
