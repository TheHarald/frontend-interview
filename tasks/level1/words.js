// Напишите функцию findShortestWord(str),
// которая принимает строку из нескольких слов и
// возвращает длину самого короткого слова в ней.
// Слова в строке разделены пробелами. Если строка пустая, верните 0.

function findShortestWord(str) {
  return 0;
}

console.log(findShortestWord("a abc ffff")); // a
console.log(findShortestWord("")); // 0
